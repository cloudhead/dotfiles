#!/bin/sh

while :; do
	status="$(systemctl --user is-active dropbox)"

	case "$status" in
		"active")
			if $(unison dropbox -testserver 2>/dev/null); then
				echo "<fc=aquamarine>✔</fc>"
			else
				echo "<fc=maroon>↯</fc>"
			fi ;;
		*)
			echo "<fc=maroon>✘</fc>" ;;
	esac

	sleep 5
done
